/**
 * Ponto único de importação para persistência mock (ELO / usuários).
 * Implementação em `lib/database.ts` — troque as importações para `@/lib/supabase` quando migrar.
 */
export * from "./lib/database"
